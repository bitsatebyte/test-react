# GI Solutions React App

---

## Components
- UserContainer
  + the user container component is a class based component used to fetch data and pass  
  it as props
- BoxComp
  + the box comp holds the content and is rendered with the fetched data
